// gulpfile.js
const gulp = require('gulp');

gulp.task('default', (done) => {
    console.log('Gulp is running...');
    done();
});
// gulpfile.js

gulp.task('default', (done) => {
    console.log('Gulp is running...');
    done();
});
